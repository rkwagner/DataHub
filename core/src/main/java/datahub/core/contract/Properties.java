package datahub.core.contract;

/**
 * Strongly-typed container for table properties, bound to a specific DBType.
 * 
 * @param <T> The specific database type these properties apply to.
 */
public class Properties<T extends DBType> {
    // Implementation details for storage will be added as we refine the property
    // key definitions
}
