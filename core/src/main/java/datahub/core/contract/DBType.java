package datahub.core.contract;

/**
 * Marker interface for specific database types (e.g., Hudi, Mongo, Postgres).
 * Implementations should define their specific property enums and
 * configurations.
 */
public interface DBType {
}
